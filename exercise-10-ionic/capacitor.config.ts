import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'exercise-10-ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
