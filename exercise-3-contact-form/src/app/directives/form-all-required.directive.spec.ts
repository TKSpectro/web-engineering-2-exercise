import { FormAllRequiredDirective } from './form-all-required.directive';

describe('FormAllRequiredDirective', () => {
  it('should create an instance', () => {
    const directive = new FormAllRequiredDirective();
    expect(directive).toBeTruthy();
  });
});
