import { DisableButtonIfNotAllInputsSetDirective } from './disable-button-if-not-all-inputs-set.directive';

describe('DisableButtonIfNotAllInputsSetDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableButtonIfNotAllInputsSetDirective();
    expect(directive).toBeTruthy();
  });
});
